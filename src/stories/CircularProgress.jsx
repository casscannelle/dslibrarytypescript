import React from 'react';
import CircularProgress from '@mui/material/CircularProgress';

const CircularProgressComponent = ({ color }) => {
  return <CircularProgress color={color} />;
};

export default CircularProgressComponent;
